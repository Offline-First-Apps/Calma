node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

# UniWind generated types
uniwind-types.d.ts

# Expo generated types. Written on `expo start`, so it turns up untracked on
# every machine that has ever run the app and must not be committed.
expo-env.d.ts

