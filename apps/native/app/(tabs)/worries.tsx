import { WorriesScreen } from '@/src/features/worry/WorriesScreen';

/** Route files read params and render one feature component. Nothing else. */
export default function Worries() {
  return <WorriesScreen />;
}
