import { CustomRatioScreen } from '@/src/features/breathing/CustomRatio';

/** Route files read params and render one feature component. Nothing else. */
export default function CustomRhythm() {
  return <CustomRatioScreen />;
}
