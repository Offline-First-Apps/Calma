import { WeekSummaryScreen } from '@/src/features/progress/WeekSummaryScreen';

/** h4. A push from h1's card, not a sixth tab. */
export default function WeekSummary() {
  return <WeekSummaryScreen />;
}
