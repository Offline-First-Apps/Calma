import { StreakRoute } from '@/src/features/progress/StreakRoute';

/** h2. Reached from h1's "Showing up" tile — see the note in StreakRoute. */
export default function Streak() {
  return <StreakRoute />;
}
