import { SearchScreen } from '@/src/features/journal/SearchScreen';

/** Route files read params and render one feature component. Nothing else. */
export default function Search() {
  return <SearchScreen />;
}
