import { SettingsScreen } from '@/src/features/settings/SettingsScreen';

/** j1. Route files read params and render one feature component. */
export default function Settings() {
  return <SettingsScreen />;
}
