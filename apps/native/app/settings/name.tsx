import { NameScreen } from '@/src/features/settings/NameScreen';

/** j1's "What we call you" row. Route files render one feature component. */
export default function SettingsName() {
  return <NameScreen />;
}
