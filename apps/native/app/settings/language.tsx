import { LanguageScreen } from '@/src/features/settings/LanguageScreen';

/** j1's language row. Route files render one feature component. */
export default function SettingsLanguage() {
  return <LanguageScreen />;
}
