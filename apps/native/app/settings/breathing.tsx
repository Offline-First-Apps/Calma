import { BreathingScreen } from '@/src/features/settings/BreathingScreen';

/** j1's "Your usual rhythm" row. Route files render one feature component. */
export default function SettingsBreathing() {
  return <BreathingScreen />;
}
