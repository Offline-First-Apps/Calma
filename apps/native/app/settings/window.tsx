import { WorryWindowScreen } from '@/src/features/settings/WorryWindowScreen';

/** j1's "Your window" row. Route files render one feature component. */
export default function SettingsWindow() {
  return <WorryWindowScreen />;
}
