import { AppearanceScreen } from '@/src/features/settings/AppearanceScreen';

/** j1's Appearance row. Route files render one feature component. */
export default function SettingsAppearance() {
  return <AppearanceScreen />;
}
