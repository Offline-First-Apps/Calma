import { AnswersScreen } from '@/src/features/settings/AnswersScreen';

/** j1's "What you told us" row. Route files render one feature component. */
export default function SettingsAnswers() {
  return <AnswersScreen />;
}
