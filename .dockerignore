node_modules
**/node_modules
.next
**/.next
.turbo
**/.turbo
.git
.env*.local
*.log

# Native-only build output. The web image never touches apps/native, but these
# directories are large and would bloat the build context if the daemon walked them.
**/android
**/ios
**/.expo

# Calma's own weight. designs/extracted/ is 116 generated HTML files and
# designs/html/ is the encoded source bundle — neither is read by next build,
# and plans/, systems/ and progress/ are gitignored working notes.
designs/extracted
plans
progress
systems
